<div class="jumbotron">
	<table class="table table-hover ">
		<%= render :partial => "versions/version", :collection => @project.versions, :as => 'version' %>
	</table>
</div>
<%= notice %>
<%= render 'versions/add_version', :version => @project.versions.build %>