<h1>Изменение информации о проекте</h1>

<%= form_for @project, :html => { role: "form" } do |f| %>
	<%= render 'form', f: f %>
  	<div class="form-group">
    	<%= f.submit "Сохранить изменения", class: "btn btn-success" %>
  	</div>
<% end %>

<%= link_to 'Вернуться', projects_path %>
