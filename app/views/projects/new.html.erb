<h4>Добавление нового проекта</h4>

<%= form_for @project, :html => { role: "form" } do |f| %>
	<%= render 'form', f: f %>
  	<div class="form-group">
    	<%= f.submit "Добавить проект", class: "btn btn-success" %>
  	</div>
<% end %>

<%= link_to 'Вернуться', projects_path %>
